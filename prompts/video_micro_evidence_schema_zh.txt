请观看这段第一人称短视频 micro-clip，输出严格 JSON，不要输出 Markdown，不要输出额外解释。

你正在为“跨会话视频记忆 benchmark”的隐藏参考标注生成短时原子证据。该输出只用于后续离线聚合、人工抽查和评测数据构建；被评测 agent 不会看到这些标注。

硬性规则：
1. 所有自然语言字段必须使用中文。
2. 只写本 micro-clip 中能从画面直接支持的信息。
3. 不能写“通常、习惯、经常、总是、always、usually、typically”等长期结论。
4. 不要推断用户偏好、领域常识、标准操作流程或长期规律。
5. 看不清、无法确认时写入 uncertainties，不要硬猜。
6. 枚举字段只能从给定集合中选择；不确定时写 "other"。
7. `object_ids`、`place_id`、`entity_id` 只能引用本 JSON 中已经声明过的 ID。
8. `clip_time_range.start_sec/end_sec` 只复制输入元信息中的原视频绝对秒数，不要转换。
9. 除 `clip_time_range` 外，事件、状态和证据时间都使用相对本 micro-clip 的 "MM:SS" 或 "MM:SS-MM:SS"，从 00:00 开始。
10. 所有相对时间都不得超过输入元信息中的 `duration_sec`。例如时长为 30 秒时，最大只能写 00:30，禁止描述相邻片段中的内容。
11. `places` 只表示可停留、移动或操作的空间区域，例如房间、操作台区域、货架过道、车内或道路区段；橱柜、抽屉、机器、车辆等可追踪实体必须写入 `objects`，不能写成地点。
12. 可移动且用于完成操作的物体归为 `tool`；固定安装或主要执行功能的设备归为 `appliance` 或 `device`；橱柜、抽屉、桌椅和固定台面归为 `furniture`；交通工具归为 `vehicle`。便携锅具、砧板、铲勺和刀具是 `tool`，固定烤箱、灶具和冰箱是 `appliance`。
13. 佩戴者的手、普通手套和衣物不得写入 `objects`，也不得写入事件的 `object_ids`。只有它们本身发生穿戴、脱下、挂起、放置或丢弃等具有记忆价值的状态变化时，才能作为对象。其他出镜人只在发生交互或后续需要追踪时记为 `person`，不得根据外观猜测身份。
14. 动作中的关键来源物体、目标容器、去向物体和发生状态变化的物体都必须先在 `objects` 中声明，并在该事件的 `object_ids` 中引用。它们优先于无关餐具或背景物体。
15. 置信度必须区分：`high` 仅用于多帧清楚可见、动作方向明确且没有合理歧义的事实；局部遮挡、类别、属性或动作方向不能完全确认时用 `medium`；只有单帧弱证据时用 `low`。如果只看到动作发生，未清楚看到变化前后状态，则该状态变化最高只能是 `medium`。不能仅因看到清洗就把“清洗前有污渍”标为 `high`，也不能仅因看到倒入就把“倒入前为空”标为 `high`。写入 uncertainties 的同一判断不能同时标为 `high`。
16. 输出前检查 `clip_summary`、`atomic_events` 和 `state_changes` 中的动作方向、来源位置和目标位置完全一致；如果互相冲突，保留证据最直接的一项，其余删除或写入 uncertainties。
17. 控制输出长度，优先保证 JSON 完整可解析。
18. 只有在多帧中清楚看到手、工具与被操作物的接触及运动，才能声称“使用某工具”。工具只是摆在附近时，不得把它写入该动作的 `object_ids`，也不得把徒手操作写成工具操作。
19. `action_type="cook"` 必须有直接加热证据，例如可见火焰、已开启的加热装置、持续冒气或沸腾，或食物熟化状态变化。只看到在锅具中放置、排列或翻动生食材时，应写 `mix`、`move` 或 `other`，并把是否正在加热写入 uncertainties。
20. 看到 `wash` 或 `clean` 动作不等于看到“脏变干净”。只有变化前的污渍与变化后的清洁状态都清楚可见时，才能写 `cleanliness` 状态变化；否则只记录清洗动作。

数量上限：
- places 最多 3 个
- objects 最多 6 个
- atomic_events 最多 4 个
- state_observations 最多 6 个
- state_changes 最多 4 个
- end_state 最多 5 个
- uncertainties 最多 3 个

输出 JSON 结构如下，字段名必须保持一致：

{
  "clip_id": "复制输入元信息中的 session_id；如果没有则 null",
  "source_video_id": "复制输入元信息中的 source_video_id 或 video_id；如果没有则 null",
  "clip_time_range": {
    "start_sec": "复制输入元信息中的 start_sec 原始秒数；如果没有则 null",
    "end_sec": "复制输入元信息中的 end_sec 原始秒数；如果没有则 null"
  },
  "clip_summary": "一句话概括本片段内可见的主要动作和关键状态变化",
  "places": [
    {
      "place_id": "place_1",
      "name": "地点或区域名称",
      "visual_anchors": ["用于之后识别该地点的画面线索"],
      "evidence_times": ["MM:SS 或 MM:SS-MM:SS"],
      "confidence": "high|medium|low"
    }
  ],
  "objects": [
    {
      "object_id": "obj_1",
      "name": "物体名称",
      "category": "food|container|tool|appliance|furniture|clothing|document|device|vehicle|personal_item|material|medicine|animal|person|other",
      "distinctive_attributes": ["颜色、形状、材质、文字、大小、位置等可追踪特征"],
      "first_seen": "MM:SS",
      "last_seen": "MM:SS",
      "initial_location": "首次清楚出现时的位置；无法判断则 null",
      "final_location": "最后清楚出现时的位置；无法判断则 null",
      "initial_state": "首次清楚出现时的状态；无法判断则 null",
      "final_state": "最后清楚出现时的状态；无法判断则 null",
      "trackability": "high|medium|low",
      "confidence": "high|medium|low"
    }
  ],
  "atomic_events": [
    {
      "event_id": "event_1",
      "time_range": "MM:SS-MM:SS",
      "action_type": "move|pick_up|put_down|open|close|use|operate|clean|cut|mix|cook|pour|wash|cover|uncover|search|inspect|store|retrieve|discard|enter|exit|travel|handover|receive|assemble|disassemble|repair|write|read|communicate|wear|take_off|eat|drink|other",
      "action": "本片段中可见动作的具体描述",
      "place_id": "place_1 或 null",
      "object_ids": ["obj_1"],
      "pre_state": "动作前相关状态；无法判断则 null",
      "post_state": "动作后相关状态；无法判断则 null",
      "evidence": "支持该事件的画面证据",
      "confidence": "high|medium|low"
    }
  ],
  "state_observations": [
    {
      "obs_id": "obs_1",
      "time": "MM:SS",
      "entity_id": "obj_1 或 place_1",
      "attribute": "location|container|possession|open_closed|on_off|condition|cleanliness|fullness|quantity|covered|configuration|task_progress|visibility|other",
      "value": "本时刻观察到的状态或位置",
      "evidence": "画面证据",
      "confidence": "high|medium|low"
    }
  ],
  "state_changes": [
    {
      "change_id": "change_1",
      "time_range": "MM:SS-MM:SS",
      "entity_id": "obj_1 或 place_1",
      "attribute": "location|container|possession|open_closed|on_off|condition|cleanliness|fullness|quantity|covered|configuration|task_progress|visibility|other",
      "before": "变化前状态；如果开始前不可见，写 unknown",
      "after": "变化后状态",
      "trigger_event_id": "event_1 或 null",
      "evidence": "画面证据",
      "confidence": "high|medium|low"
    }
  ],
  "end_state": [
    {
      "entity_id": "obj_1 或 place_1",
      "attribute": "location|container|possession|open_closed|on_off|condition|cleanliness|fullness|quantity|covered|configuration|task_progress|visibility|other",
      "value": "片段结束时可见或可直接推出的状态",
      "evidence_time": "MM:SS",
      "confidence": "high|medium|low"
    }
  ],
  "uncertainties": [
    {
      "time_range": "MM:SS 或 MM:SS-MM:SS",
      "item": "无法确认的实体、动作、文字、声音或状态",
      "reason": "不确定原因",
      "impact": "该不确定性影响哪些状态或事件"
    }
  ]
}

抽取重点：
1. 优先写物体位置、物体状态、状态变化和片段结束状态。
2. 先保证每个关键事件涉及的来源、目标和状态变化物体都已声明并正确引用，再考虑补充背景物体。
3. 同类餐具很多时，只保留对状态变化或后续追踪重要的物体。
4. 不能因为一个片段看到某物在某处，就写成“通常放在某处”。
5. 输出必须是一个可被 json.loads 解析的 JSON 对象。
