# Ego4D 烹饪试点 12 条局部证据抽取分析

## 结论

Qwen3.5-35B-A3B 对第一人称烹饪动作的主体识别质量较好，12 条响应均正常结束且
JSON 可解析。但本次输入片段由旧版直接码流拷贝生成，受关键帧和 GOP 边界影响，
实际时长普遍超过清单声明的 30 秒，造成相邻片段内容进入当前证据。因此这 12 条结果
只能用于发现流程问题，不能进入后续窗口聚合或参考证据。

修复后应重新精确切分，并重新抽取同一组 12 条作为质量门禁；在此之前不运行剩余
333 条试点片段。

## 样本与运行状态

- 来源视频：`032f0478-17c5-4ac3-845c-0dd87c2ef113`
- 覆盖范围：来源视频前 6 分钟
- 模型：Qwen3.5-35B-A3B
- 采样：每秒 1 帧
- 输出：12/12 为 `ok`，12/12 的结束原因均为 `stop`
- 输入词元：7,495 到 10,179，平均 8,966
- 输出词元：1,833 到 2,792，平均 2,244

## 切片边界问题

旧版直接拷贝切片的实际容器时长如下：

| 片段 | 计划时长 | 实际时长 |
| --- | ---: | ---: |
| s000 | 30 秒 | 30.15 秒 |
| s001 | 30 秒 | 44.64 秒 |
| s002 | 30 秒 | 34.51 秒 |
| s003 | 30 秒 | 39.44 秒 |
| s004 | 30 秒 | 30.77 秒 |
| s005 | 30 秒 | 30.39 秒 |
| s006 | 30 秒 | 44.77 秒 |
| s007 | 30 秒 | 43.52 秒 |
| s008 | 30 秒 | 36.89 秒 |
| s009 | 30 秒 | 41.50 秒 |
| s010 | 30 秒 | 40.27 秒 |
| s011 | 30 秒 | 39.02 秒 |

新校验器按清单中的 30 秒上限检查后，7/12 条被拒绝，共发现 67 个越界时间字段。
典型污染包括：

- `s009` 的目标范围是 270 到 300 秒，主体应为切肉，但模型还抽取了前一段的搅拌酱汁；
- `s011` 的目标范围是 330 到 360 秒，主体应为装盘，但模型记录了相对 30 到 38 秒的
  后续倒酱动作；
- `s006` 和 `s007` 分别包含目标边界之后的取铲、挤番茄酱等动作，造成相邻片段重复。

对问题片段 `s001`、`s006`、`s009`、`s011` 使用重编码重新切分后，实际时长均为
30.000 秒。

同一风险也存在于旧 EPIC-KITCHENS 流程。对 `P30_113` 连续抽查 12 个直接拷贝
片段，实际时长为 30.25 到 36.26 秒，其中 11 条超过 0.25 秒容差。因此旧版
Kitchen micro 输出也需要按精确切片重新抽取。

## 模型内容质量

排除切片问题后，模型有以下优点：

- 能识别倒入、清洗、取出、测温、翻取肉饼、挤酱、搅拌、切肉和装盘等主要动作；
- 能稳定生成物体位置、前后状态、片段结束状态和不确定项；
- 没有长期偏好或习惯推断，没有英文漂移；
- 局部编号唯一，来源视频编号和主要引用关系正确。

需要改进的地方：

- 215 个置信度字段全部为 `high`，置信度没有校准；
- 橱柜、抽屉、冰箱有时被放入地点，而不是家具或电器对象；
- 便携锅具在 `tool`、`container` 和 `appliance` 之间分类不一致；
- 4 条结果把操作者的手套作为普通物体，占用了有限对象名额；
- 个别事件在文字中提到目标碗或目标容器，但没有在对象表声明并建立引用；
- 数量上限较常被用满：事件 5/12、对象 3/12、状态变化 2/12、地点 2/12、
  不确定项 2/12。当前不提高上限，先通过优先级规则减少无关对象。

## 流水线修复

本轮代码调整包括：

1. 固定片段默认使用精确重编码，质量参数为 CRF 23；
2. 状态表和推理 URL 表记录实际时长、误差和校验状态；
3. 未通过时长校验的片段不会写入推理清单，旧的未校验记录不会被续跑逻辑接受；
4. 局部证据校验器拒绝超过片段时长的相对时间；
5. 提示词明确空间区域、家具、电器和便携工具的边界，要求关键来源与目标物体建引用；
6. 提示词增加置信度分级，并默认排除与任务无关的手、手套和衣物。

## 放行条件

修复后的同一组 12 条必须同时满足：

- 12 个切片全部通过实际时长校验；
- 12 个模型响应全部为可解析 JSON，且没有越界时间；
- 主体动作与精确片段画面一致；
- 置信度不再机械地全部为 `high`；
- 关键来源、目标和状态变化对象均有声明与引用。

通过后再运行 15 个来源视频的全部 345 条局部抽取，然后进入 120 秒窗口聚合。
